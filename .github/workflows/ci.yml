name: test backend

on:
  push:
    branches:
      - '**'

jobs:
  frontend-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: oops_trap/oops_trap
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: some logs
        run: pwd

      - name: setup node.js 
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: oops_trap/oops_trap/package-lock.json

      - name: install dependencies
        run: npm ci

      - name: run eslint
        run: npm run lint
      
      - name: build project
        run: npm run build

      - name: upload build
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: oops_trap/oops_trap/dist/
          retention-days: 1
      
  frontend-test:
    runs-on: ubuntu-latest
    needs: frontend-build
    defaults:
      run:
        working-directory: oops_trap/oops_trap
    steps:
      - name: test checkout
        uses: actions/checkout@v4

      - name: setup node.js 
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: oops_trap/oops_trap/package-lock.json

      - name: install dependencies
        run: npm ci

      - name: run unit tests
        run: npm run test:unit

      - name: run tests with coverage
        run: npm run test:coverage

      - name: upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: oops_trap/oops_trap/coverage/
          retention-days: 1

  frontend-e2e:
    runs-on: ubuntu-latest
    needs: frontend-build
    defaults:
      run:
        working-directory: oops_trap/oops_trap
    steps:
      - name: checkout 
        uses: actions/checkout@v4

      - name: setup node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: oops_trap/oops_trap/package-lock.json
      
      - name: install dependencies
        run: npm ci

      - name: download build 
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: oops_trap/oops_trap/dist
    
      - name: install playwright
        run: npx playwright install --with-deps

      - name: Run e2e tests
        run: npm run test:e2e
