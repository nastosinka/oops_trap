name: test backend

on:
  push:
    branches: [main]

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - name: checout code
      - uses: actions/checkout@v3
      - run: echo "test test test"
